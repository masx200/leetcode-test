class GridMaster {
    constructor(
        grid: number[][],
        r1: number,
        c1: number,
        r2: number,
        c2: number,
    ) {
    }
    canMove(direction: string): boolean {
    }
    move(direction: string): number {
    }
    isTarget(): boolean {
    }
}
export { GridMaster as GridMaster };
